# hive connector — reads parquet files from minio via hive metastore
connector.name=hive

hive.metastore.uri=thrift://hive-metastore:9083

# minio endpoint (docker service name, not localhost)
hive.s3.endpoint=http://minio:9000

# must match MINIO_ROOT_USER / MINIO_ROOT_PASSWORD in .env
hive.s3.aws-access-key=minioadmin
hive.s3.aws-secret-key=minioadmin

# path-style required for minio — virtual-hosted style won't work
hive.s3.path-style-access=true

# no SSL locally
hive.s3.ssl.enabled=false

hive.storage-format=PARQUET
hive.allow-drop-table=true
parquet.use-column-names=true
