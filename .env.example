# copy to .env and change values — don't commit .env

# postgres
POSTGRES_USER=demo
POSTGRES_PASSWORD=demo
POSTGRES_DB=appdb

# minio (must match hive-site.xml and lake.properties)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# trino — no auth in local mode, UI at http://localhost:8080
# TRINO_PORT=8080
